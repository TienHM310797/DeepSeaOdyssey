using UnityEngine;
using System.Collections;

public class coinEffControl : MonoBehaviour
{
    void Stasssrt()
    {
        LeanTween.move(gameObject, new Vector2(-5.6f, 3.4f), 1).setOnComplete(() =>
        {
            Destroy(gameObject);
        });
        AudioControl.cvbdtfg.coinsdfe();
    }    void Stassrt()
    {
        LeanTween.move(gameObject, new Vector2(-5.6f, 3.4f), 1).setOnComplete(() =>
        {
            Destroy(gameObject);
        });
        AudioControl.cvbdtfg.coinsdfe();
    }    void Sasdtart()
    {
        LeanTween.move(gameObject, new Vector2(-5.6f, 3.4f), 1).setOnComplete(() =>
        {
            Destroy(gameObject);
        });
        AudioControl.cvbdtfg.coinsdfe();
    }

    void Start()
    {
        LeanTween.move(gameObject, new Vector2(-5.6f, 3.4f), 1).setOnComplete(() =>
        {
            Destroy(gameObject);
        });
        AudioControl.cvbdtfg.coinsdfe();
    }


}
